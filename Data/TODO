Use Cosmos DB Locally.

Implement Application Repository, take db interface through DI.

Add CosmosDb through DI.

Set environment configuration in Cosmos DB (or use the service? Get the environment from the service, make sure mathes the data project?)

Interface database class that can be implemented by both cosmos and entity.
        Make demo a base class, inherit and configure db values there.

Apply validation logic to cosmos db in order to make sure transactions are saved properly.