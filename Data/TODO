Implement other common repo functions.

Implement DB and Repo interfaces.

DI Inject values into controller w/ Settings (test by hard coding contorller logic).

Make Angular application create service calls.

Validate values coming in from the service.

Use more complex data structures and use CRUD.

Push to production.

Apply validation logic to cosmos db in order to make sure transactions are saved properly.